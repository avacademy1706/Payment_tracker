# Base URL for API requests. Leave unset in development — Vite's dev server
# proxies /api to the backend (see vite.config.ts), so no value is needed.
# Set this only for a production build served from a different origin than the API.
VITE_API_URL=
